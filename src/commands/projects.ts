import type { Command } from "commander";
import type { ConfigManager } from "../config/manager.js";
export function registerProjectsCommand(_program: Command, _configManager: ConfigManager): void {
  // Implemented in Task 7
}
